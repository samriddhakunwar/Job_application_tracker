# Base URL of the backend REST API (includes the /api prefix).
# On Vercel this is provided automatically as NEXT_PUBLIC_BACKEND_URL=/api,
# so you only need this for local development.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
